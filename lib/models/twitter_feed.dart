class TwitterFeed {
  TwitterFeed({this.tweetText, this.userName, this.tweetId, this.userId, this.profilePicURL});
  final String tweetText;
  final String userName;
  final int tweetId;
  final String userId;
  final String profilePicURL;
}
